# Screenshots

This folder contains screenshots captured from the AWS console while building the **AWS Network Connectivity Project**.

Suggested files:

- `vpc-layout.png` – VPC and subnet layout.
- `route-tables.png` – Route table configuration.
- `connectivity-test.png` – Connectivity test results.
- `security-rules.png` – Security Group and NACL rules.
